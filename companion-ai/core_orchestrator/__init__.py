"""core_orchestrator — Main orchestration service for companion-ai.

Runs on port 8000 as the central LangGraph-based state machine
that coordinates persona, memory, voice, action, device, and gateway modules.
"""

__version__ = "0.1.0"
